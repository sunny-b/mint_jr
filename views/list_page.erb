<div class="incomes">
  <h2 class="category-name"><%= @page_name %></h2>
  <table>
    <tr>
      <th>Type</th>
      <th>Amount</th>
    </tr>
    <% @list.each do |item| %>
      <tr>
        <td><%= item[:type] %></td>
        <td><%= item[:amount] %></td>
        <td>
          <form action="/<%= @page_name.downcase %>/<%= item[:id] %>/delete" method="post">
            <button type="submit">Delete</button>
          </form>
        </td>
      </tr>
    <% end %>
    <tr>
      <form action="/<%= @page_name.downcase %>/add" method="post">
        <td>
          <input name="type" placeholder="<%= @item_description %>" type="text">
        </td>
        <td>
          <input name="amount" placeholder="100, 200, etc." type="text">
        </td>
        <td>
          <input type="submit" value="Add">
        </td>
      </form>
    </tr>
  </table>
</div>

<% content_for :header_links do %>
  <a class="main" href="/">Back to Main Menu</a>
  <h5 class="logo">Mint JR.</h5>
<% end %>
